# Teaching Notes

- Direct spinoff of [[../quantum-computing/NOTES.md|Quantum Computing]] — same user, same strong CS/linear-algebra background, opened by that course's still-locked Shor's algorithm lesson (lesson 7 there) rather than a standalone request. Lesson 3 here is the load-bearing cross-link: it must connect cleanly to `quantum-computing/lessons/0006-quantum-fourier-transform-phase-estimation.html` (QFT and phase estimation) and to the locked `quantum-computing/index.html#lesson-07` card.
- Strong existing foundation from Quantum Computing and Category Theory: modular arithmetic mechanics can move fast, but this course should still work every example with real, small numbers by hand — no "and so on" — since that's the house style established across every topic and is especially important for a subject as easy to hand-wave as cryptographic hardness assumptions.
- Same rigor-over-rhetoric standard as Quantum Computing and Logic: be precise about what Shor's algorithm actually does (period-finding via phase estimation over modular exponentiation) versus popular oversimplifications ("quantum computers break all encryption").
- RSA worked examples use the classic textbook pair p=61, q=53 (n=3233, φ(n)=3120, e=17, d=2753) — small enough to compute by hand via square-and-multiply, matching the numbers used in most standard cryptography textbooks and Wikipedia's own worked RSA example, so the user can cross-check against other sources without the numbers looking unfamiliar.
- Mission explicitly scopes out AES/symmetric crypto and deep hash-function treatment — public-key crypto and its number theory is the throughline to quantum computing; keep lessons from drifting into general applied-crypto engineering (side channels, constant-time code) per the mission's "out of scope."
