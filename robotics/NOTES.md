# Teaching Notes

- 2026-08-25: Zero prior robotics/controls background. Explicitly asked for
  "a nice rundown" before diving into tuning — start every lesson from
  first principles, don't assume physics/controls vocabulary.
- Comfortable software engineer (owns and edits the source repo directly)
  — safe to lean on programming/systems analogies rather than
  physics-first ones.
- 2026-08-26: This curriculum was migrated here from the source project's
  own `learning/` directory (originally a standalone `/teach` workspace
  inside `robot-feasibility-framework`), so it could be hosted on GitHub
  Pages without exposing that repo's other (private, business-sensitive)
  content. Converted to this hub's shared design system, quiz engine, and
  navigation conventions in the process — content and citations carried
  over unchanged.
- Has a working `unitree_mujoco` sim locally and can re-run
  `scripts/run_stand_mission.py` themselves — lessons should push toward
  testing against the real sim, not stop at quizzes.
